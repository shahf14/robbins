# ── AI ───────────────────────────────────────────────────────────────────────
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_LIFE_COACH_STRUCTURING_MODEL=gpt-4.1-mini
OPENAI_LIFE_COACH_DAILY_STEPS_MODEL=gpt-4.1-mini
OPENAI_LIFE_COACH_REVIEW_MODEL=gpt-4.1
OPENAI_FORMULATION_EXPLORATION_MODEL=gpt-4.1-mini
OPENAI_FORMULATION_DRAFT_MODEL=gpt-4.1-mini
OPENAI_FORMULATION_GOAL_MODEL=gpt-4.1
# Default per-user AI API quota for routes without a route-specific override.
AI_RATE_LIMIT_MAX_REQUESTS=20
AI_RATE_LIMIT_WINDOW_SECONDS=3600
# When true, formulation exploration uses deterministic fallback instead of LLM.
FORMULATION_DETERMINISTIC_EXPLORATION=false

# ── Clerk (Authentication) ────────────────────────────────────────────────────
# Get from https://dashboard.clerk.com → your app → API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Where Clerk redirects after sign-in / sign-up (legacy /sign-in redirects to /{locale}/sign-in)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# Clerk webhook secret (Webhooks → your endpoint → Signing Secret)
CLERK_WEBHOOK_SECRET=whsec_...

# ── App ───────────────────────────────────────────────────────────────────────
# Generate: openssl rand -hex 32
LIFE_COACH_CRON_SECRET=

# Local/self-host fallback auth (required in production/Docker).
# Generate: openssl rand -hex 32
# Paste the same value in Settings → Local API authentication in the browser.
LOCAL_USER_ID=local-user
LOCAL_USER_EMAIL=
LOCAL_AUTH_TOKEN=
# Fail process start in production when LOCAL_AUTH_TOKEN/Clerk are missing (enabled in docker-compose).
STRICT_AUTH_BOOT=false
# Opt-in only: allow open API access on localhost (127.0.0.1 / localhost).
# Works in development and in local Docker (NODE_ENV=production) on loopback only.
ALLOW_LOCAL_AUTH=true

# Admin access (email of the admin user — must match signed-in Clerk user or LOCAL_USER_EMAIL)
ADMIN_EMAIL=
# Required for /api/db/* and /api/logs in production. Optional on localhost when
# NODE_ENV=development or ALLOW_LOCAL_AUTH=true (dev bypass + session cookie).
ADMIN_API_TOKEN=

# Keep secrets outside synced folders (e.g. not in OneDrive project root).
